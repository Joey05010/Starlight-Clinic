// Function to run when the document is fully loaded
document.addEventListener('DOMContentLoaded', function() {
    // Console log for confirmation that the script is linked correctly
    console.log("Starlight Clinic Homepage loaded successfully! JS is running.");

    // Placeholder for future interactivity, e.g., a mobile navigation toggle:
    
    /* const mobileMenuButton = document.querySelector('.menu-icon');
    const navBar = document.querySelector('.navbar');

    if (mobileMenuButton) {
        mobileMenuButton.addEventListener('click', function() {
            navBar.classList.toggle('active');
        });
    }
    */
});